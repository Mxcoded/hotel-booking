{
    "$schema": "https://json.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "echo \"No build tools configured — using CDN for CSS/JS.\""
    }
}
